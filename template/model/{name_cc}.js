/*
Summary: This model contains {{name_cc}} schema.
*/
const mongoose = require('mongoose');

const {{name_cc}}Schema = new Schema({});

let model{{name_pc}} = mongoose.model('{{name}}', {{name_cc}}Schema);
module.exports = model{{name_pc}};